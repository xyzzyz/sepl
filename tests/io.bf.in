#include "io.h"

void hello(;locals ; arrays [int] arr ;) {
  arr = "Hello, World\n";
  call print_string(arr);
  return;
};

void main(; locals ; arrays ;) {
  call hello(;arrays 100);
  call print_int(6667);
  output '\n';
  return;
};

